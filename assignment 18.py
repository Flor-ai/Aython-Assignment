def concat_tuples(tuple1, tuple2):
    # Use the + operator to concatenate both tuples
    return tuple1 + tuple2

# Example usage
tuple1 = (1, 2, 3)
tuple2 = (4, 5)

result = concat_tuples(tuple1, tuple2)
print(f"Concatenated tuple: {result}")
